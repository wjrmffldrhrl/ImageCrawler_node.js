doctype html
html
    head
        meta(charset='utf-8')
        title IMG Crawler

    body
        h1 IMG Crawler

        form(action="/target", method="post")
            input(type="text", name="url", placeholder="input target url")
            button(type="submit") Start

        - var tableCnt = 0;

        if useAble
            table
                each image in content
                    - tableCnt = tableCnt+1
                    - table4 = tableCnt%4
                    if table4 == 0
                        tr
                    td    
                        img(src=image, width="200", height="200", border="0")

        else
            h2 Not URL
            
